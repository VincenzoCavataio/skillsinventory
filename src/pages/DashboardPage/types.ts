export type TecnologyData = { id: number, name: string }
export type EducationalLevelData = { id: number, name: string }
export type CompiledFields = {
    fullName?: string;
    skill?: string;
    certification?: string;
    city?: string
};
