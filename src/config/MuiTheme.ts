import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#8CBE2D",
    },
    secondary: {
      main: "#white",
    },
  },
});

export default theme;
